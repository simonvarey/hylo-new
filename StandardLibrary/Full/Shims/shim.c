#include <stdio.h>
#include <stdint.h>

// The full standard library doesn't yet support compilation due to conformances requiring projection lowering
// Once that's resolved, this file can be used to provide C implementations of some stdlib functions.
